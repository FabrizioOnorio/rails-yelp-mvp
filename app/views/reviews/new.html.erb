<h3>Add a new review:</h3>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>
<%= link_to 'Back to the restaurant', restaurant_path(params[:restaurant_id]) %>
